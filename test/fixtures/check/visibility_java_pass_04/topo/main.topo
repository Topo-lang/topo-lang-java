// Visibility check disabled (mode=off).  Cross-class private call would
// normally error, but is suppressed.

namespace app {
  public:
    void pipeline();

    type Lib {
      public:
        void publicEntry();
      private:
        void hidden();
    }

    type Consumer {
      public:
        void invoke();
    }

    fn pipeline {
        stage<1> app::Lib::publicEntry();
    }
}
